spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto
        concurrency: ${bench.concurrency:2}
        max-concurrency: ${bench.maxConcurrency:8}
        prefetch: ${bench.prefetch:10}
