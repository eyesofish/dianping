# TODO 状态总览（截至 2026-03-05）

说明：本文件按你原来的问题 1~9 逐条核对当前代码，标记为 `已完成` / `部分完成` / `未完成`。

---

## 已完成

1. 问题1（CacheClient 分布式锁重构，使用 Redisson / watch dog）
- 状态：已完成
- 证据：
  - `src/main/java/com/hmdp/utils/CacheClient.java`
  - 使用 `RedissonClient` + `RLock`，在缓存重建逻辑里 `tryLock(0, TimeUnit.SECONDS)`，由 Redisson 管理锁续期。

2. 问题2（项目启动缓存预热）
- 状态：已完成
- 证据：
  - `src/main/java/com/hmdp/runner/CacheWarmUpRunner.java`（`CommandLineRunner`）
  - `src/main/java/com/hmdp/service/CacheWarmUpService.java`（`warmUpShopCache()`）

3. 问题3（去掉 `@Resource`，改为构造器注入）
- 状态：已完成
- 证据：
  - 主代码中已无 `@Resource`（全局搜索）。
- 备注：
  - 仍有 1 处字段注入：`CacheWarmUpRunner` 使用 `@Autowired` 字段注入，建议后续也改成构造器注入（非阻塞问题）。

4. 问题4（Lua keys 参数化）
- 状态：已完成
- 证据：
  - `src/main/java/com/hmdp/service/impl/VoucherOrderServiceImpl.java`
    - `stringRedisTemplate.execute(SECKILL_SCRIPT, Arrays.asList(stockKey, orderKey, streamKey), ...)`
  - `src/main/resources/seckill.lua`
    - 使用 `KEYS[1..3]` 获取 `stockKey/orderKey/streamKey`，不再在 Lua 中硬编码 key。

7. 问题7（RefreshTokenInterceptor ThreadLocal 清理）
- 状态：已完成
- 证据：
  - `src/main/java/com/hmdp/interceptor/RefreshTokenInterceptor.java`
  - 已实现 `afterCompletion()` 并调用 `UserHolder.removeUser()`。

---

## 部分完成

6. 问题6（MQ 消费稳定性、重试/死信、重复下单风险）
- 状态：部分完成
- 已完成部分：
  - `QA/QD` 消费者已统一接入 `handleVoucherOrder()`，锁逻辑进入主消费链路。
  - 证据：`src/main/java/com/hmdp/listener/SeckillVoucherListener.java`
- 未完成部分：
  - 未看到显式的 ACK 策略配置（如手动 ACK / 自定义重试策略）。
  - 未看到“死信队列消费前按 `orderId` 幂等检查”的专门逻辑。
  - 未看到订单表唯一约束（如 `user_id + voucher_id`）在代码/迁移中明确落地。

---

## 未完成

5. 问题5（MQ 丢消息兜底：pending 集合 + 定时补偿）
- 状态：未完成
- 现状：
  - 未发现 `seckill:pending:order` 集合写入/删除逻辑。
  - 未发现 `@Scheduled` 补偿任务。

8. 问题8（Shop 新增/更新时同步写入 GEO）
- 状态：未完成
- 现状：
  - `ShopController.saveShop()` 只写 DB。
  - `ShopServiceImpl.update()` 只做 DB 更新 + 删除缓存，未同步 GEO。
  - 查询 `shop/of/type` 的 GEO 路径依赖 `shop:geo:<typeId>`，但写入链路缺失。

9. 问题9（前端保存商铺时缺地址/坐标）
- 状态：未完成
- 现状：
  - 当前前端页面没有商铺新增/编辑页（`frontend/hmdp/src/pages` 无对应页面）。
  - “地址 + 地图选点 + 提交 lng/lat”的链路尚未接入。

---

10.把imple 类对象都改成父类引用形式，例如：`ShopServiceImpl` -> `ShopService`

## 下一步优先级建议

1. 先做问题8（GEO 写入链路），否则附近搜索结果会长期不准。
2. 再做问题5（MQ 丢消息兜底），提高秒杀订单最终一致性。
3. 最后补问题6剩余项（ACK 策略 + 死信幂等 + DB 唯一约束）。
